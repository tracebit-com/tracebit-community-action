# generated-types

The types in this directory are generated/synced from an external source, and should not be edited manually.

## deployment-strategy.ts

This file is synced/generated from the internal Tracebit repository, and contains the types used to define deployment strategies for perimeter instances.
This is designed to be forward-compatible with the API implementation, so only needs to be synced when the action needs to use new strategies/fields.
